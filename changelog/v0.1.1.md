# v0.1.1 — 2026-04-21

## Fixed

- `claude` binary lookup when the app is launched from a desktop entry (e.g. GNOME Activities) with a trimmed `$PATH`. Falls back to common install locations — `~/.local/bin`, `/usr/local/bin`, `~/.claude/local` — when `$PATH` doesn't contain it.

## Notes

- No user-visible feature changes.
